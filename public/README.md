# Public Assets Folder

This folder contains high-resolution static images used across the FixIAI landing page.
You can drop your custom mockups, screenshots, and visual assets here to replace the default ones.
